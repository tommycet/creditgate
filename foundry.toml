[profile.default]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

# RPC endpoints (set COSTON2_RPC_URL in .env)
[rpc_endpoints]
coston2 = "https://coston2-api.flare.network/ext/C/rpc"

[profile.default.fuzz]
runs = 256
