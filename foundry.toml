[profile.default]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

[profile.default.rpc_endpoints]
coston2 = "${COSTON2_RPC_URL}"

[profile.default.fuzz]
runs = 256
